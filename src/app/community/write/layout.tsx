export const metadata = {
  title: "Monico - 새 글 쓰기",
  description: "새 글을 쓸 수 있는 페이지",
};

const CommunityWriteLayout = ({ children }: { children: React.ReactNode }) => {
  return <div>{children}</div>;
};

export default CommunityWriteLayout;
